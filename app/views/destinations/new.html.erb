<h1>Add a new destination!</h1>

<%= render "errors/flash_errors" %>
<%= form_for @destination do |f| %>
  <%= f.label :city %>
  <%= f.text_field :city %>
  <%= f.collection_select :airline_ids, @airlines, :id, :name %>
  <%= f.submit %>
<% end %>
